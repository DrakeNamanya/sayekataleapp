import 'package:flutter/material.dart';

class SHGPurchaseInputsScreen extends StatelessWidget {
  const SHGPurchaseInputsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Purchase Inputs')),
      body: const Center(child: Text('Buy Seeds, Fertilizers & Equipment')),
    );
  }
}
